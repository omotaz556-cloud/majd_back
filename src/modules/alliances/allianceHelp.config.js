// ====== إعدادات "مساعدة التحالف" (Alliance Help) ======
// نفس فلسفة alliance.config/repair.config: أرقام Placeholder مبدئية
// قابلة للتعديل من هنا من غير ما تلمس أي منطق تاني.

// أقصى عدد أعضاء مختلفين يقدروا يساعدوا في نفس طلب المساعدة الواحد -
// لما يوصل العدد ده الطلب بيتقفل (status='completed') حتى لو لسه فاضل وقت.
const MAX_HELP_COUNT = 5;

// كل ضغطة "مساعدة" بتقلل الوقت المتبقي بالقد ده (بالثواني) - مينفعش
// الوقت المتبقي ينزل تحت صفر (بيتوقف عند "دلوقتي" لو الرقم أكبر من الوقت
// الفعلي المتبقي).
const HELP_SECONDS_REDUCTION = 60;

module.exports = {
  MAX_HELP_COUNT,
  HELP_SECONDS_REDUCTION,
};
