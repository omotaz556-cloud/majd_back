/**
 * ====== مزوّد الحساب: Majd Platform (لسه مش متصل) ======
 *
 * بيتفعّل لما ACCOUNT_PROVIDER=majd_platform. الهدف من الملف ده إنه يكون
 * المكان الوحيد اللي هيتعدل لما تفاصيل التكامل مع حساب منصة مجد تتحدد
 * (الشكل النهائي للتوكن/الطلب، رابط الـ API، طريقة التحقق...). مفيش حاجة
 * تانية في المشروع محتاجة تتغيّر - auth.controller و auth.service وكل
 * الموديولات التانية (castle, games, wallet, ...) بتتعامل مع نفس شكل
 * الملف الشخصي الموحّد اللي بيرجعه `authenticate()` بالظبط زي ما بيرجعه
 * local.provider.js.
 *
 * ====== اللي محتاج يتحدد قبل التنفيذ الفعلي ======
 *   - رابط الـ API بتاع منصة مجد للتحقق من الحساب (مثلاً عن طريق متغيّر بيئة
 *     زي MAJD_PLATFORM_API_URL)
 *   - المفتاح/السر اللي المنصة هتستخدمه للتحقق من إن الطلب جاي من اللعبة دي
 *     فعلاً (مثلاً MAJD_PLATFORM_API_KEY)
 *   - شكل الـ "credentials" اللي هتوصل من الفرونت إند لـ authenticate() -
 *     ممكن تكون توكن جلسة من المنصة نفسها (الأرجح)، أو حتى إيميل/باسورد
 *     بيتبعتوا للمنصة عشان تتحقق منهم بدل ما نتحقق إحنا محلياً
 *
 * ====== register() مش موجودة عمداً ======
 * الحساب أصلاً موجود على منصة مجد - مفيش تسجيل جديد من جوا اللعبة، وده
 * بالظبط سبب طلب التكامل ده. auth.controller بيتأكد من كده ويرفض بوضوح
 * لو حد حاول ينادي /register والمزوّد الفعّال مش local.
 */

async function authenticate(/* credentials */) {
  throw new Error(
    'ACCOUNT_PROVIDER=majd_platform لسه مش متصل فعلياً - التفاصيل ' +
      '(رابط API منصة مجد، شكل التوكن، طريقة التحقق) لسه محتاجة تتحدد. ' +
      'نفّذ authenticate() هنا لما تبقى جاهزة، من غير أي تعديل في أي مكان تاني.'
  );
}

module.exports = { authenticate };
